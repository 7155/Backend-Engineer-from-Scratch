# Backend 教材协作说明

## 教材形式必须模仿 Juliepy

- 顶层使用 `00-Computer`、`01-Web`、`02-Database` 这类编号章节。
- 章内使用 `01-xxx.py → 02-xxx.py → 03-xxx.md/sql` 顺序推进。
- 一个编号文件只解释一个核心概念，学习者应能从 01 顺序运行到最后。
- 优先写可独立运行的 `.py` / `.sql`；代码注释承担主要教学职责。
- README 只负责学习目标、顺序、运行入口和章末复述，不重复代码正文。
- 不为每个知识点创建目录和成套 Markdown。测试统一放在根级 `tests/` 或脚本中。
- Notebook 只在交互实验明显更合适时使用。

## 固定学习链

```text
最简单方案 → 触发问题 → 工程演化 → 原理 → 最小代码 → 可视化 → Saleor → Trade-off → 面试 → 费曼复述
```

## Engineering Evolution

- 数据库、缓存、MQ、并发、交易、服务架构、可观测性和部署必须有编号 `*_evolution.md`。
- 每次演化都写清 V0 为什么合理、触发信号、第一次优化、新故障、当前方案和停止升级条件。
- 禁止把版本差异写成“旧=落后、新=先进”；只能根据需求、规模、组织和部署约束解释权衡变化。
- 强制回答“为什么不是一开始就这样”和“什么时候不该使用高级方案”。
- Saleor 历史结论必须标注 tag/commit 和文件证据；推断必须明确写成推断。
- 全局模板见 `ENGINEERING_EVOLUTION.md`。

## 证据来源优先级

1. 权威教材、标准和官方文档：负责定义、机制和已知边界。
2. 可运行实验：负责把结论变成可观察现象。
3. 固定版本的生产源码：负责展示真实约束和工程取舍。
4. AI：只负责重新组织、解释、连接、出题，不充当未经验证的事实来源。

架构演化优先参考《凤凰架构》官方站点；数据库优先 PostgreSQL 官方文档与 CMU 15-445/645；Redis、RabbitMQ、Kafka、Celery、Kubernetes 和 OpenTelemetry 优先各自官方文档。统一索引见 `SOURCE_INDEX.md`。禁止大段复制参考资料；未被来源、实验或源码支撑的结论必须标记“需要验证”。

Visual Lab 必须说明：输入什么、点击/执行什么、观察什么、为什么变化、一个费曼问题。优先链接已核验的开源工具，不复制第三方代码；需要本地可视化时只做离线、自包含、单概念的 HTML。

## Saleor 边界

- Saleor 只放在章后半段或 `90-saleor_mapping.md`，不能代替原理教材。
- 映射前固定 tag/commit，沿入口、业务函数、模型、事务/锁、异步事件和测试追踪。
- 区分已核验源码事实、推断、数据库实验和生产运行证据。
- 第三方源码只放被忽略的 `.references/`，不修改、不推送。

## 学习互动

进入某个编号文件时，先让用户说已有理解；每轮只纠正 1～2 个关键错误；用具体输入走代码；最后只问一个复述题。除非用户明确要求完成实现，否则保留学习目标代码给用户手写。
