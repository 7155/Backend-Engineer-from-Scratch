# Backend 教材协作说明（Course Builder v3.1）

## Canonical Contract

本仓库的 **README 就是教材正文**。学习者不需要先去翻源码，才能理解核心机制。

固定教学链：

```text
现实问题 → 最简单可运行方案 → 观察失败 → 引入一个机制
→ 用具体值走执行过程 → 核心代码带读 → 状态/不变量
→ 失败与取舍 → Saleor 3.23.25 映射 → 练习 → 费曼复述
```

## README 规则

每章 README 至少包含：

1. 这一章解决什么真实问题；
2. 一个贯穿全章的 TinyCommerce 例子；
3. 至少一张 Mermaid 图，难点优先两张；
4. 可运行的最小实验与预期现象；
5. 5–15 行核心代码摘录，按真实执行顺序解释；
6. 执行前/执行后状态变化；
7. 失败、并发、超时、重试或缓存失效等相关边界；
8. Saleor `3.23.25` 的真实源码映射；
9. 练习题与一个费曼复述题。

禁止把解释拆到“自己去源码看”。源码路径是证据和继续探索入口，不是缺失解释的替代品。

## 不再生成面试题

本仓库不建立 interview question bank，也不写“30 秒面试回答”“面试追问”。
所有检查理解的内容统一改成：

```text
手算题 / 运行题 / 故障实验 / 代码修改题 / 设计题 / 费曼复述
```

## Engineering Evolution

所有核心机制都回答：

- V0 为什么在小规模下合理；
- 哪个可观察问题逼出 V1；
- V1 改善了什么；
- 新方案引入什么新问题；
- 什么规模下不值得继续升级；
- Saleor 当前在哪里使用了这个机制。

禁止写成“旧方案落后，新方案先进”。只能根据请求量、数据量、失败成本、组织协作和部署约束解释权衡。

## 证据优先级

1. 可运行代码、测试、固定版本源码；
2. 实际命令/trace/benchmark；
3. 项目官方文档；
4. 权威标准与一手资料；
5. 模型推断，必须标记为推断。

Saleor 固定使用 tag `3.23.25`。不根据当前 `main` 猜测 3.23.25 的实现。

## 学习互动

进入某章时，优先让学习者先解释已有理解，再纠正 1–2 个关键错误。使用具体输入和状态变化，不先讲框架 API。每轮最后只留一个主费曼题。
